<ips:template parameters="$collab" />
<header class="collabHeader{{if ! \IPS\collab\Application::urlMatch( $collab )}} collabHeadMinified{{endif}}">
	{$collab->coverPhoto()|raw}
	{template="collabStatBar" app="collab" group="components" params="$collab"}
</header>